// Rendered in the `@drawer` slot when the URL has no token segment (the bare chain page)
// and on any hard navigation, where intercepting routes do not apply.
export default function Default(): null {
	return null;
}
